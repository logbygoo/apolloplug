export const CONFIG = {
  // Set to 'OFF' to enable the under construction page with password protection.
  // Set to 'ON' to make the site publicly accessible.
  siteStatus: 'OFF' as 'ON' | 'OFF',

  // The password to access the site when siteStatus is 'OFF'.
  accessPassword: 'itiiitii12'
};
